{
  "name": "@game-judge/api-worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml --var ENVIRONMENT:development --var FIB_WORD_PROVIDER:local --var SENTRY_DSN:",
    "dev:test": "wrangler dev --config wrangler.e2e.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "db:migrate:local": "wrangler d1 migrations apply werewolf-db --local --config wrangler.toml",
    "db:migrate:remote": "wrangler d1 migrations apply werewolf-db --remote --config wrangler.toml",
    "db:seed:local": "node ../../scripts/seed-local.mjs",
    "types": "wrangler types worker-configuration.d.ts --config wrangler.toml --env-interface WorkerBindings --strict-vars=false",
    "types:check": "wrangler types worker-configuration.d.ts --config wrangler.toml --env-interface WorkerBindings --strict-vars=false --check",
    "typecheck": "pnpm run types:check && tsc --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.54.0",
    "@game-judge/game-engine": "workspace:*",
    "bcryptjs": "^3.0.3",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.23",
    "jose": "^6.2.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "@cloudflare/vitest-pool-workers": "~0.13.5",
    "vitest": "~4.1.7",
    "wrangler": "^4.95.0"
  }
}
