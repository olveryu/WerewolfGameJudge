{
  "name": "@werewolf/api-worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:test": "wrangler dev --config wrangler.test.toml",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply werewolf-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply werewolf-db --remote",
    "db:seed:local": "node ../../scripts/seed-local.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.54.0",
    "@werewolf/game-engine": "workspace:*",
    "bcryptjs": "^3.0.3",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.23",
    "jose": "^6.2.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "~0.13.5",
    "@cloudflare/workers-types": "^4.20260528.1",
    "vitest": "~4.1.7",
    "wrangler": "^4.95.0"
  }
}
