/**
 * Mock for @expo/vector-icons/Ionicons (direct subpath import).
 * Re-exports the Ionicons mock from the barrel mock as default export.
 */
import { Ionicons } from './index';

export default Ionicons;
