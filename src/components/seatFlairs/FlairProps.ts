/** Props for all seat flair animation components */
export interface FlairProps {
  /** Tile inner size (px) — playerTile width/height */
  size: number;
  /** Border radius of the tile (matches playerTile) */
  borderRadius: number;
}
